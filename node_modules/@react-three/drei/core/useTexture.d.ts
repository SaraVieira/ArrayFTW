import { Texture } from 'three';
export declare function useTexture<Url extends string[] | string>(url: Url): Url extends any[] ? Texture[] : Texture;
export declare namespace useTexture {
    var preload: (url: string) => undefined;
}
