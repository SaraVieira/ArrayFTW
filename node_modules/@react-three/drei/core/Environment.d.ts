import { Scene } from 'three';
declare type Props = {
    background?: boolean;
    files?: string | string[];
    path?: string;
    preset?: string;
    scene?: Scene;
};
export declare function Environment({ background, files, path, preset, scene, }: Props): null;
export {};
